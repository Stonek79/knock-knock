export const USER_CONTACTS_MODES = {
    LIST: "list",
    SELECT: "select",
} as const;

export const USER_WEB_STATUS = {
    ONLINE: "online",
    OFFLINE: "offline",
    TYPING: "typing",
} as const;
