# Тестирование Knock-Knock

> Последнее обновление: Апрель 2026
> Статус: 🏗 В процессе (рефакторинг + покрытие)

---

## 📊 Стратегия тестирования

Проект использует многоуровневый подход к обеспечению качества, разделенный по средам исполнения и типам инструментов.

### 1. Среды исполнения (Environments)

| Среда | PocketBase URL | Контейнер | Назначение |
|-------|----------------|-----------|------------|
| **Development (Dev)** | `https://dev-api.knok-knok.ru:8443` | `knok-knok-pb-dev` | Разработка, Unit-тесты, ручное ломание |
| **Production (Prod)** | `https://api.knok-knok.ru:8443` | `knock-knock-pb` | Реальные пользователи, высокая стабильность |

---

## 🛠 Инструментарий

### 1. Тестирование логики (Unit-тесты, Vitest)
- **Цель**: Проверка чистых функций, сложной логики сервисов и сторов в изоляции.
- **Инструментарий**: Vitest + **Моки репозиториев**. Прямое мокирование SDK PocketBase запрещено.
- **Преимущества**: Высокая скорость (миллисекунды), детерминизм, тестирование граничных случаев (ошибки сети).
- **Команды**:
  ```bash
  cd app
  npm run test          # Запуск всех Unit-тестов
  npm run test:watch    # Режим разработки (Unit)
  ```

### 2. Интеграционное тестирование (Real DB, Vitest)
- **Цель**: Проверка **Репозиториев**, корректности схемы БД, работы серверных хуков и миграций.
- **Инструментарий**: Vitest (`vitest.integration.config.ts`) + **Реальный PocketBase (Dev/Staging)**.
- **Принцип**: Создание реальных записей с флагом `is_test: true`.
- **Команды**:
  ```bash
  cd app
  npm run test:integration # Запуск интеграционных тестов (на реальной БД)
  ```

### 2. UI-тестирование (Storybook)
- **Цель**: Изолированная разработка и визуальное тестирование компонентов.
- **Команда**: `npm run storybook`

### 3. E2E-тестирование (Playwright)
- **Цель**: Проверка критических путей пользователя (Auth, Messaging, Crypto).
- **Среда**: Запускается против Dev-окружения.
- **Команды**:
  ```bash
  cd app
  npx playwright test
  npx playwright test --ui  # С визуальным интерфейсом
  ```

### 4. Линтинг и типы (Biome + tsc)
- **Biome**: `npm run lint` — линтинг + форматирование.
- **TypeScript**: `npx tsc --noEmit` — проверка типов без сборки.
- **Подробнее**: `/run-tests` workflow в `.agent/workflows/run-tests.md`.

---

## 🏗 Настройка тестового окружения (Dev/Staging)

### 1. Запуск контейнера
Для тестов и разработки используется отдельный инстанс PocketBase:
```bash
# На сервере
cd ~/knok-knok-bd-dev
docker compose -f docker-compose.dev.yml up -d
```
Порт на хосте: `9090` -> мапится через Nginx на `dev-api.knok-knok.ru`.

### 2. Переменные окружения (.env.test)
Для запуска тестов локально создайте `.env.test` (не коммитится):
```bash
VITE_PB_URL=https://dev-api.knok-knok.ru:8443
PB_ENCRYPTION_KEY=...ваша_соль...
```

### 3. Сидирование данных
Для наполнения базы тестовыми пользователями используйте скрипт:
```bash
cd app
npm run seed
```

---

## 📝 Написание тестов: Лучшие практики (Repository Pattern)

1. **Unit-тесты (Сервисы)**: Мокаем репозитории через `vi.mock("@/lib/repositories/...")`. Фокусируемся на логике трансформации данных и бизнес-правилах.
2. **Integration-тесты (Репозитории)**: Пишем в `.integration.test.ts`. Используем реальный SDK и БД. Обязательно выставляем `is_test: true`.
3. **Используем Result-обертку**: Тесты должны учитывать, что методы возвращают `Result<T, AppError>`.
4. **Storybook**: Компоненты документируются и тестируются визуально через Storybook (`npm run storybook`).
5. **Biome перед коммитом**: `npm run lint` обязателен. Настроен pre-commit хук (Husky).

**Пример разделения:**
- `uploadMedia.test.ts` (Unit): Мокаем репозиторий, проверяем, что сервис вызывает шифрование перед загрузкой.
- `media.repository.integration.test.ts` (Integration): Выполняем реальный `uploadMedia`, проверяем, что файл появился в PocketBase.

---

## 🧹 Очистка данных

Для интеграционных тестов используется механизм **мягкой очистки**:
1. Все тестовые записи помечаются `is_test: true`.
2. Хелпер `cleanupDatabase()` (вызывается в `beforeAll` интеграционных тестов) удаляет только эти записи.
3. Это гарантирует, что тесты не удалят стабильные данные для разработки (сиды).
