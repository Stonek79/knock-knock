# Инструкции агентов

Единственный актуальный набор общих правил находится в корневом
[`AGENTS.md`](../AGENTS.md). Ролевые `agent.json` обязаны сначала применять его.

Этот файл оставлен только для инструментов, которые автоматически ищут
инструкции внутри `.agents/`.
